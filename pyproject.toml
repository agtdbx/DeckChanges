[project]
name = "deckchanges"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Agtdbx", email = "auguste.deroubaix@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "customtkinter>=6.0.0",
    "pillow>=12.3.0",
    "pyinstaller>=6.22.2",
    "requests>=2.34.2",
]

[project.scripts]
deckchanges = "deckchanges:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
